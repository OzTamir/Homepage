// Site-wide static configuration.

// Base URL of the blog (Astro). The `~/posts` section fetches
// `${BLOG_URL}/posts.json` and links its heading at `${BLOG_URL}/`.
export const BLOG_URL = "https://posts.oztamir.com";
